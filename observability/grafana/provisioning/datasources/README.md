Grafana datasource provisioning for this stack lives in `default.yaml`.

Current provisioned datasources:
- Prometheus
- Loki

These datasources are intended to come up automatically on Grafana startup in the local observability stack.