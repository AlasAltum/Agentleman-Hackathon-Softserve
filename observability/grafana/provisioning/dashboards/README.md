Dashboard provisioning is optional in the first iteration.

The provider manifest lives in `default.yaml` and points Grafana to the `definitions` directory.

Add dashboard JSON files under `definitions/` when dashboards are ready.