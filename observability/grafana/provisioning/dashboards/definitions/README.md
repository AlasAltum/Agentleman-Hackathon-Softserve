Place Grafana dashboard JSON files here when the first dashboards are created.

This directory is already wired into the dashboard provider manifest, but dashboards remain optional in the first iteration.