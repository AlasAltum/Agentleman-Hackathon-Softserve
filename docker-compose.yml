name: gentleman-stack

# Include the backend and observability sub-stacks from the repository root.
include:
  - path: backend/docker-compose.yml
    project_directory: backend
    env_file: ./.env
  - path: observability/docker-compose.yml
    project_directory: observability
  # - path: sre-platform/docker-compose.yml
  #   project_directory: sre-platform
  #   env_file: ./.env
  # - path: ecommerce-platform/docker-compose.yml
  #   project_directory: ecommerce-platform
  #   env_file: ./.env

# Root-level overrides can still be added here if needed.
services: {}
