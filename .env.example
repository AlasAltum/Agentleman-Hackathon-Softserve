# all required environment variables with placeholders and comments

# Grafana admin credentials for the observability stack.
# These values are consumed by observability/docker-compose.yml with defaults,
# so local direct runs can still fall back to admin/admin if needed.
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=admin

# Local MLflow settings for the observability stack.
# Test scripts running from the host should use localhost.
MLFLOW_TRACKING_URI=http://localhost:5000
MLFLOW_EXPERIMENT_NAME=observability-local


# 
ATLASSIAN_EMAIL="alonso.utreras@ug.uchile.cl"
ATLASSIAN_API_TOKEN=""
JIRA_BASE_URL="https://hackathon-softserve-ecommerce.atlassian.net"
JIRA_PROJECT_KEY="KAN"
CONFLUENCE_BASE_URL="https://hackathon-softserve-ecommerce.atlassian.net/wiki/"
CONFLUENCE_SPACE_KEY="SRE"

# ZAVU for SMS, WSP and others.
# https://dashboard.zavu.dev/
SMS_ID=
ZAVU_API_KEY=

# 
GEMINI_API_KEY=
OPENROUTER_API_KEY=



EXPOSED_APP_PORT=
APP_PORT=
POSTGRES_USER= 
POSTGRES_PASSWORD= 
POSTGRES_PORT=
POSTGRES_BDNAME= 
