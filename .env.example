# all required environment variables with placeholders and comments

# Grafana admin credentials for the observability stack.
# These values are consumed by observability/docker-compose.yml with defaults,
# so local direct runs can still fall back to admin/admin if needed.
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=admin

# Local MLflow settings for the observability stack.
# Test scripts running from the host should use localhost.
MLFLOW_TRACKING_URI=http://localhost:5001
MLFLOW_EXPERIMENT_NAME=observability-local

# Prometheus scrape target for the smoke-test metrics emitter.
# The default expects the Docker-based observability-test-runner service.
# Override this only if you intentionally run emit_metrics.py outside Docker.
OBSERVABILITY_METRICS_TARGET=observability-test-runner:9464


# 
ATLASSIAN_EMAIL="alonso.utreras@ug.uchile.cl"
ATLASSIAN_API_TOKEN=""
JIRA_BASE_URL="https://hackathon-softserve-ecommerce.atlassian.net"
JIRA_PROJECT_KEY="KAN"
JIRA_ISSUE_TYPE="Task"
JIRA_DEFAULT_LABELS="sre,observability"
JIRA_TIMEOUT_SECONDS=15
# Optional: use this if your Jira workflow resolves issues with a custom transition name.
JIRA_RESOLVED_TRANSITION_NAME="Done"
CONFLUENCE_BASE_URL="https://hackathon-softserve-ecommerce.atlassian.net/wiki/"
CONFLUENCE_SPACE_KEY="SRE"

# NYLAS email notifications.
NYLAS_API_KEY=
NYLAS_GRANT_ID=
NYLAS_EMAIL_ADDRESS=alerts@example.com
NYLAS_EMAIL_REPLY_TO=alerts@example.com
NYLAS_TEAM_EMAIL_RECIPIENTS=sre-team@example.com
NYLAS_BASE_URL=https://api.us.nylas.com/v3
NYLAS_TIMEOUT_SECONDS=15
NYLAS_INCLUDE_TICKET_URL=false

# API Keys used by our agents. We use Gemini since it has
# A big context window, it is relavitely cheap and has good performance
GEMINI_API_KEY=
OPENROUTER_API_KEY=


# For the backend application, the one that receives the /ingest endpoint
# And that processes workflow. These defaults also let the root compose file
# parse cleanly when only observability services are started.
EXPOSED_APP_PORT=8000
APP_PORT=8000
POSTGRES_USER=postgres
POSTGRES_PASSWORD=localdev
POSTGRES_PORT=5432
POSTGRES_BDNAME=postgres
